#include <stdio.h>
int main() { 
    int area, altura, base;
        printf("digite a altura do triangulo:\n") ;
        scanf("%i", &altura);
        printf("digite a base do triangulo:\n");
        scanf("%i", &base);
        printf("%d", altura * base / 2, &base);
        input("Pressione ENTER para finalizar");
}